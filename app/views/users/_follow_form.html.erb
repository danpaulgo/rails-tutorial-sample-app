<!-- form_for tag will create form using attributes from the object created using this build method (same as "Relationship.new" with the follower_id preassigned) and be "posted" to the "/relationships" path which is routed to the create action -->
<%= form_for(current_user.active_relationships.build) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>